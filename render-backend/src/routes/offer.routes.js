const express = require('express');
const router = express.Router();

// Placeholder routes for offer management
router.get('/', (req, res) => {
  res.json({ message: 'Offer routes - To be implemented' });
});

module.exports = router;
