const express = require('express');
const router = express.Router();

// Placeholder routes for room management
router.get('/', (req, res) => {
  res.json({ message: 'Room routes - To be implemented' });
});

module.exports = router;
